SeedApp.service('UserModel', function() {


	this.login = function(user.email, user.password) {
		
		
		

		if (!user) {
			user = { userId: "0", firstName: "", lastName: "", theme: "" }
		}

		user.push(user);
		<!-- window.localStorage.setItem(user, JSON.stringify(user)); -->
	};


});
